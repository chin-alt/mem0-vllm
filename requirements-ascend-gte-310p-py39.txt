# Ascend 310P GTE reranker inference stack for Python 3.9.
#
# Target compatibility line:
#   Host driver: Ascend HDK 24.1.RC2.x
#   CANN:        8.0.RC2 (toolkit + 310P kernels)
#   Python:      3.9.x
#   PyTorch:     2.1.0 + torch-npu 2.1.0.post6
#
# Install torch and torch-npu from matching cp39 wheels first. Then install this
# file with --no-deps for torch/torch-npu if the cloud mirror tries to replace
# either wheel.
--extra-index-url https://mirrors.huaweicloud.com/ascend/repos/pypi

setuptools==68.2.2
wheel==0.41.3
decorator==5.1.1
packaging==24.2

torch==2.1.0
torch-npu==2.1.0.post6
numpy==1.26.4
protobuf==3.20.3

# The Ascend GTE migration practice pins these three versions. Do not upgrade
# this environment to Transformers 5.x.
transformers==4.39.1
tokenizers==0.15.1
huggingface-hub==0.20.3
safetensors==0.4.3
einops==0.8.0

# Business Evaluation Matrix dependencies.
pandas==2.2.3
openpyxl==3.1.5
scikit-learn==1.5.2
scipy==1.13.1
tqdm==4.66.5
requests==2.32.3
PyYAML==6.0.2
